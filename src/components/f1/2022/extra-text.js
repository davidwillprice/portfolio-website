import React, { Fragment } from "react"

import { tick, cross } from "../../../styles/component/f1/stats.module.scss"

export const helpNotes = <Fragment></Fragment>

export function statsNotes(isSeasonOver) {
  return <Fragment></Fragment>
}
